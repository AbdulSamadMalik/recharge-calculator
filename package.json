{
  "name": "recharge-calculator",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "tsec-compile-check": "tsec -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "sass": "^1.43.4",
    "tsec": "^0.1.11",
    "typescript": "^4.3.2",
    "vite": "^2.5.2"
  },
  "dependencies": {
    "rxjs": "^7.4.0"
  }
}
